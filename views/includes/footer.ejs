<footer class="footer-hireska">
  <div class="container">
    <div class="footer-grid">

      <!-- Brand col -->
      <div class="footer-brand-col">
        <div class="footer-brand-mark">
          <div class="brand-mark" style="transform: scale(0.8); transform-origin: left center;">
            <div class="brand-shape-main"></div>
            <div class="brand-shape-accent"></div>
          </div>
          <div>
            <div class="footer-brand-name">Hireska</div>
            <div class="footer-brand-tagline">People move possibilities</div>
          </div>
        </div>
        <p class="footer-brand-desc">
          A modern recruitment platform that combines human conversations, real skills, and AI to build a more open, fair, and opportunity-driven future.
        </p>
        <div class="footer-socials">
          <a href="#" class="social-pill" aria-label="LinkedIn">
            <i class="fab fa-linkedin"></i>
          </a>
          <a href="#" class="social-pill" aria-label="Twitter">
            <i class="fab fa-twitter"></i>
          </a>
          <a href="#" class="social-pill" aria-label="Email">
            <i class="fa-solid fa-envelope"></i>
          </a>
        </div>
      </div>

      <!-- Platform links -->
      <div class="footer-col">
        <div class="footer-col-label">Platform</div>
        <ul class="footer-links">
          <li><a href="/home">Home</a></li>
          <li><a href="/jobs">Job Board</a></li>
          <li><a href="/questions/list">Questions</a></li>
          <li><a href="/recruiter/register">For Employers</a></li>
        </ul>
      </div>

      <!-- Account links -->
      <div class="footer-col">
        <div class="footer-col-label">Account</div>
        <ul class="footer-links">
          <li><a href="/users/register">Register</a></li>
          <li><a href="/users/login">Login</a></li>
          <li><a href="/users/profile">Profile</a></li>
        </ul>
      </div>

      <!-- System info -->
      <div class="footer-col">
        <div class="footer-col-label">Technology</div>
        <div class="footer-sys-card">
          <div class="sys-row">
            <span class="sys-key">Stack</span>
            <span class="sys-val">Node · Express</span>
          </div>
          <div class="sys-row">
            <span class="sys-key">Video</span>
            <span class="sys-val">WebRTC</span>
          </div>
          <div class="sys-row">
            <span class="sys-key">AI Engine</span>
            <span class="sys-val">Gemini 1.5</span>
          </div>
          <div class="sys-row">
            <span class="sys-key">Status</span>
            <span class="sys-val sys-live"><span class="sys-dot"></span>All systems operational</span>
          </div>
        </div>
      </div>

    </div>

    <!-- Bottom bar -->
    <div class="footer-bottom">
      <span class="footer-copy">© <%= new Date().getFullYear() %> <strong>Hireska</strong>. All rights reserved.</span>
      <span class="footer-coords">More than hiring. A fairer tomorrow.</span>
    </div>
  </div>
</footer>

<style>
/* ===============================================
   FOOTER — HIRESKA EDITION
   =============================================== */
.footer-hireska {
  background: var(--bg-tertiary);
  margin-top: 100px;
  padding: 80px 0 32px;
  position: relative;
  border-top: 1px solid var(--border-color);
}

.footer-grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1.5fr;
  gap: 64px;
  margin-bottom: 64px;
}

/* Brand column */
.footer-brand-mark {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 24px;
}

.footer-brand-name {
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 700;
  color: var(--green-primary);
  letter-spacing: -0.02em;
}

.footer-brand-tagline {
  font-family: var(--font-primary);
  font-size: 12px;
  color: var(--text-secondary);
  margin-top: 2px;
}

.footer-brand-desc {
  font-family: var(--font-primary);
  font-size: 15px;
  color: var(--text-secondary);
  line-height: 1.6;
  margin-bottom: 24px;
  max-width: 90%;
}

.footer-socials {
  display: flex;
  gap: 12px;
}

.social-pill {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-secondary);
  font-size: 16px;
  transition: all 0.2s ease;
  text-decoration: none;
}

.social-pill:hover {
  background: var(--green-primary);
  border-color: var(--green-primary);
  color: #fff;
  transform: translateY(-2px);
  box-shadow: var(--shadow-sm);
}

/* Link columns */
.footer-col-label {
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 600;
  color: var(--green-primary);
  margin-bottom: 24px;
}

.footer-links {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.footer-links a {
  font-family: var(--font-primary);
  font-size: 15px;
  color: var(--text-secondary);
  text-decoration: none;
  transition: all 0.2s ease;
}

.footer-links a:hover {
  color: var(--green-action);
}

/* System info card */
.footer-sys-card {
  display: flex;
  flex-direction: column;
  gap: 12px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: 24px;
}

.sys-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--border-color);
  gap: 12px;
}
.sys-row:last-child {
  padding-bottom: 0;
  border-bottom: none;
}

.sys-key {
  font-family: var(--font-primary);
  font-size: 13px;
  font-weight: 500;
  color: var(--text-muted);
}

.sys-val {
  font-family: var(--font-primary);
  font-size: 13px;
  color: var(--text-primary);
  text-align: right;
  font-weight: 500;
}

.sys-live {
  color: var(--green-action);
  display: flex;
  align-items: center;
  gap: 6px;
}

.sys-dot {
  width: 8px;
  height: 8px;
  background: var(--green-action);
  border-radius: 50%;
  animation: pulse-live 2s ease-in-out infinite;
}

@keyframes pulse-live {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.5; transform: scale(0.8); }
}

/* Bottom bar */
.footer-bottom {
  padding-top: 32px;
  border-top: 1px solid var(--border-color);
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 16px;
}

.footer-copy {
  font-family: var(--font-primary);
  font-size: 14px;
  color: var(--text-muted);
}

.footer-copy strong {
  color: var(--green-primary);
}

.footer-coords {
  font-family: var(--font-display);
  font-size: 14px;
  color: var(--text-secondary);
  font-style: italic;
}

/* Responsive */
@media (max-width: 1024px) {
  .footer-grid {
    grid-template-columns: 1fr 1fr;
    gap: 48px;
  }
  .footer-brand-col {
    grid-column: 1 / -1;
  }
}

@media (max-width: 600px) {
  .footer-grid {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .footer-bottom {
    flex-direction: column;
    align-items: flex-start;
  }
}
</style>
