<nav class="w-full flex items-center justify-between px-6 md:px-12 py-3 glass backdrop-blur-md bg-white/5 border-b border-white/10 text-white shadow-sm rounded-2xl">
  <!-- 🔰 Brand -->
  <div class="text-2xl font-bold tracking-wide">
    <a href="/home" class="hover:text-blue-300 transition-all">⚡ InterViewX</a>
  </div>

  <!-- 🔗 Links -->
  <div class="flex items-center gap-5 sm:gap-6 text-sm sm:text-base font-medium">
    <a href="/jobs" class="hover:text-blue-400 transition"><i class="fa-solid fa-briefcase"></i> Jobs</a>
    <a href="/recruiter/register" class="hover:text-blue-400 transition">🏢 Recruiter Register</a>

    <% if (user) { %>
      <!-- 👤 Show Chats only when logged in -->
      <a href="/chats/chatbox/<%= user._id %>" class="flex items-center gap-1 bg-blue-600/30 hover:bg-blue-600/60 px-3 py-1.5 rounded-full backdrop-blur-md border border-blue-400/50 transition text-white font-semibold">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M7 8h10M7 12h8m-5 8l-4-4H5a2 2 0 01-2-2V7a2 2 0 012-2h14a2 2 0 012 2v6a2 2 0 01-2 2h-3l-4 4z" />
        </svg>
        <span>Chats</span>
      </a>
    <% } %>

    <% if (!user) { %>
      <!-- 🧑 Guest Links -->
      <a href="/users/login" class="hover:text-blue-400 transition">🔐 Login</a>
      <a href="/users/register" class="hover:text-blue-400 transition">📝 Register</a>
    <% } else { %>
      <!-- 👤 User Profile -->
      <a href="/users/profile" class="flex items-center gap-2 bg-white/10 px-3 py-1.5 rounded-full backdrop-blur-md border border-white/20 hover:text-blue-300 transition">
        <span class="text-lg">👨</span>
        <span class="font-medium text-white">Profile</span>
      </a>

      <!-- 🚪 Logout -->
      <a href="/users/logout">Logout ⍈</a>
    <% } %>
  </div>
</nav>
