<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Select Question Type</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #1f0036, #3b0066);
      color: #fff;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .container {
      background: rgba(255, 255, 255, 0.06);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.15);
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
      max-width: 500px;
      width: 90%;
      text-align: center;
    }

    h1 {
      font-size: 26px;
      margin-bottom: 30px;
      color: #ffffff;
    }

    .button-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
    }

    .button-grid a {
      text-decoration: none;
      background: linear-gradient(135deg, #6a00f4, #9f7bff);
      padding: 14px 20px;
      border-radius: 12px;
      color: white;
      font-size: 18px;
      font-weight: 500;
      transition: all 0.3s ease;
      box-shadow: 0 6px 20px rgba(159, 123, 255, 0.3);
    }

    .button-grid a:hover {
      background: linear-gradient(135deg, #7c2fff, #b88cff);
      transform: translateY(-3px);
      box-shadow: 0 10px 24px rgba(159, 123, 255, 0.5);
    }

    @media (max-width: 500px) {
      .button-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>🧠 What type of question do you want to add?</h1>
    <div class="button-grid">
      <a href="/questions/new/mcq">➕ Add MCQ</a>
      <a href="/questions/new/saq">➕ Add SAQ</a>
    </div>
  </div>

</body>
</html>
