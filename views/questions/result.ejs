<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

<div class="min-h-screen bg-gradient-to-br from-[#1f0036] to-[#3b0066] text-white flex flex-col items-center justify-center px-6 py-20">

  <!-- Feedback Box -->
  <div class="max-w-xl w-full bg-white/5 backdrop-blur-md border border-white/10 rounded-xl p-8 shadow-2xl text-center space-y-6">

    <!-- Result -->
    <h1 class="text-3xl font-bold tracking-wide
      <% if (isCorrect) { %> text-green-400 <% } else { %> text-red-400 <% } %>">
      <%= isCorrect ? "✅ Correct Answer!" : "❌ Wrong Answer" %>
    </h1>

    <!-- Question -->
    <% if (question) { %>
      <p class="text-lg text-gray-200">Q: <span class="font-medium"><%= question.questionText %></span></p>
    <% } %>

    <!-- Error Message -->
    <% if (errorMessage) { %>
      <p class="text-red-400 font-medium"><%= errorMessage %></p>
    <% } %>

    <!-- Back Button -->
    <a href="/home"
       class="inline-block px-5 py-2 bg-gradient-to-r from-purple-600 to-violet-500 hover:from-purple-700 hover:to-violet-600 transition rounded-lg text-white font-semibold shadow-lg">
      ← Back to All Questions
    </a>
  </div>
</div>

</body>
</html>
